const meuArray = [];
let contador = 0;

while (contador <= 100) {
	meuArray.push(contador);
	contador++;
}

// Fazer uma função que soma todos os valores ímpares do array e retorna em um popup (alert)
+
let soma=0;
	for (let numImpar=1; numImpar<=100;numImpar++){
		if(numImpar %2!=0){
			/*console.log(numImpar);*/
			soma= soma+numImpar;
	}
}
 alert(" A Soma dos números Impares de 0 à 100 é :"+soma);

//MEU TRABALHO-CHARLES GODOY